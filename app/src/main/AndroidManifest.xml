<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group_7_proj">
    
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->


        <activity
            android:name=".LoginActivity"
            android:label="Login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".JobCatSearchResultActivity"
                android:label="Search Results">
        </activity>
        <activity
            android:name=".JobTextSearchResultActivity"
            android:label="Search Results">
        </activity>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".JobPostActivity" />
        <activity android:name=".PaymentInfoActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ChooseJobPrefActivity" />
        <activity android:name=".JobPreferenceMatchActivity" />
        <activity android:name=".JobPostViewActivity">
        </activity>
    </application>

</manifest>